[project]
name = "rollout-server"
version = "0.1.0"
description = "Example RolloutServer using Osmosis SDK"
requires-python = ">=3.11"

dependencies = [
    "osmosis-ai[server]==0.2.8",
]

[dependency-groups]
dev = [
    "mypy>=1.8.0",
    "ruff>=0.2.0",
]
